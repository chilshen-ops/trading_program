
## 🔄 实盘 / 模拟交易切换

| 模式 | 环境变量 | 说明 |
|------|----------|------|
| **实盘** | 不设置 `SIMULATE` | 连接币安真实账户 |
| **模拟** | `SIMULATE=true` | 虚拟资金，不真实下单 |

---

## 📁 核心文件

| 文件 | 作用 |
|------|------|
| `trader.py` | 执行程序（v18.0） |
| `.sim_state.json` | 模拟账户余额+持仓+止损单持久化 |
| `blacklist.json` | 黑名单（禁止交易的下架币种） |
| `~/.openclaw/workspace/trading_program/memory/` | 预判验证记录 + 交易记录 + 多空胜率|
| `lg.md` | 交易逻辑 只更新不新建，确保唯一 |

---

## 📋 交易命令

```bash
cd ~/.openclaw/workspace/trading_program

# =============================================
# 扫描候选币种（动态策略：见 lg.md → [策略名称]）
# =============================================
python3 trader.py scan-all --top {scan_top} --klines {scan_klines} --min-vol {scan_min_vol}

# =============================================
# 账户 & 市场数据
# =============================================
python3 trader.py status                        # 账户状态
python3 trader.py market --symbol=BTCUSDT     # 市场数据
# =============================================
# LLM 分析
# =============================================
python3 trader.py llm-hold --symbol=XXX      # LLM分析持仓
python3 trader.py llm-open --symbol=XXX       # LLM分析开仓
# =============================================
# 交易执行（实盘无需 SIMULATE=true）
# =============================================
python3 trader.py open-long 10 --symbol=XXX --leverage=10      # 开多
python3 trader.py open-short 10 --symbol=XXX --leverage=10      # 开空
python3 trader.py cancel-conditionals --symbol=XXX                   # 取消指定币种全部追踪委托
python3 trader.py cancel-conditionals --symbol=XXX --side=SELL        # 只取消某一方向
python3 trader.py close-long --symbol=XXX                                         # 平多
python3 trader.py close-long --symbol=XXX --percent=50                        # 平多(减仓50%)
python3 trader.py close-short --symbol=XXX                                        # 平空
python3 trader.py close-short --symbol=XXX --percent=50                       # 平空(减仓50%)
```

## ⚠️ open-long / open-short 命令格式（关键！）

```bash
# ✅ 正确格式：保证金是位置参数，放在命令名后面
python3 trader.py open-long 10 --symbol=XXX --leverage=10
                            ^ 位置参数，不带 --


# ❌ 错误格式：--margin 会报 "unrecognized arguments"
python3 trader.py open-long --margin 10 --symbol=XXX
```


| 参数 | 必须 | 类型 | 说明 |
|------|------|------|------|
| `margin` | ✅ | 位置参数 | **保证金金额（USDT）**，不带 `--` |
| `--symbol` | 是 | 选项 | 币种，如 `NILUSDT` |
| `--leverage` | 否 | 选项 | 杠杆倍数，默认 `10` |

---

## ⚙️ 策略执行框架（6步骤，每步执行完需回复"已执行"）

> **SKILL.md = 执行框架（what to do）**，**lg.md = 推理逻辑（how to think）**
> **lg.md 是分析框架**——策略条件、禁止事项、输出格式全部在 lg.md 中定义。LLM 裸看原始K线+成交量，按 lg.md 的逻辑判断是否满足条件，不满足就不做。
> SKILL.md 只负责调度：获取数据 → 加载lg → 执行lg分析 → 下单执行 → 迭代lg → 汇报。
> **修改 lg.md 后，SKILL.md 无需任何改动。**

---

### 步骤1：获取数据

> 纯技术操作，不分析，只收集。**先获取持仓状态，根据持仓数决定是否扫描候选币种。**

#### 1.1 账户数据 + 判断需要什么数据
```bash
python3 trader.py status
```
- 实盘/虚拟账户、**总资产（记录备用，用于仓位换算）**、持仓币种、盈亏、持仓数

**判断流程**：
```
持仓数 == 0  → 空仓：执行1.2扫描候选币种 + 1.4获取全量memory → 对候选币种逐一分析（步骤3）
持仓数 < 上限 → 有仓但未满：执行1.2扫描（同时管理现有持仓）+ 1.4获取全量memory
持仓数 >= 上限 → 满仓：跳过扫描，只管理现有持仓 + 1.4获取全量memory
```

#### 1.2 扫描候选币种（空仓/未满仓时执行）

```bash
python3 trader.py scan-all --top 50 --klines 50 --min-vol 5.0
```

**策略：见 lg.md → [策略名称]** — 程序输出全市场波动率最高的50个币，每个币附带1h×12 + 30m×8 K线数据，LLM 自行按 lg.md 的筛选条件判断。

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `--top` | 50 | 最终输出币种个数 |
| `--klines` | 50 | 给多少个候选拿K线 |
| `--min-vol` | 5.0 | 最低1h波动率%（降低到3.0可看到更多币）|


**扩大扫描范围：**
- 扩大 `--top`（50→100），看更多币
- 降低 `--min-vol`（5.0→3.0），接受更低波动币


> **满仓时跳过此步**，直接进入仓位管理流程。

#### 1.3 市场数据（补数据用）

```bash
python3 trader.py market --symbol=XXX
```
- **仅当** scan-all 的输出数据不够用时才调用（如数据过期了、或你想看更详细的周期）
- **扫描候选场景**：直接复用 scan-all 的数据，不需要每个币再调一次 market
- **持仓巡检场景**：获取持仓币种的实时K线数据供步骤3分析

#### 1.4 全量历史记忆（预判验证 + 交易记录）

```bash
cat ~/.openclaw/workspace/trading_program/memory/*.md
```

> **核心原则**：记忆记录的是你过去的预判和交易结果，用来帮你改进**预判能力**，不是限制你入场的。每轮必须从候选币种中选出一个进行重点监控（可交易可观察），保持对市场的跟踪不间断。**你的预判准确率是在实战中练出来的，连续观察也是在积累判断力。最终判断以当前K线数据为准，记忆只是参考。**

**读取记忆的目的**（不是找不做的理由，是看预判能力怎么提高）：
1. **看预判准确率** — 对每笔历史的预判验证，看自己的预判能力在什么水平。方向预判准确率越高说明判断力在进步，方向常错说明要调整分析角度
2. **看预判偏差模式** — 是方向判断问题（趋势识别错），还是时机问题（方向对但入场点不对），还是持仓问题（方向对但没拿住）？找到自己的弱项，这轮重点改进
3. **按币种看预判记录** — 当前币种过去预判了什么、实际怎么走的、预判准确度如何？这次分析时避开同样的偏差
4. **看交易定性** — 上次交易是"依据成立的正常交易"还是"预判有偏差的试错"？正常交易亏损→市场问题无需自责；试错亏损→预判方法需要改进
5. **多空胜率统计** — 只用于方向参考，**无论胜率高低都要基于当前数据独立判断**
6. **上次重点事项** — 上次说下次要在哪个环节做更好？这次做到了吗？

**机会驱动思维**：
- 核心问题不是"我错在哪里"，而是"这次是否符合 lg.md → [策略名称] 的条件"
- 符合条件 → 验证预判，大胆做
- 不符合条件 → 果断不做，不要强行找理由做
---

### 步骤2：读取lg策略

> 加载并理解当前的交易逻辑框架。

1. **读取 `lg.md` 全文**，理解当前交易逻辑框架
2. **理解消化内容**：理解总结后再执行下一步
3. **候选币种**：识别到暴涨币种但没掉头的加入候选币种，等待掉头机会
---

### 步骤3：分析（执行lg.md逻辑）

> **所有分析在 lg.md 中完成，SKILL.md 不做任何分析。**
> 读取 lg.md 全文，按 lg.md 自身的分析框架执行。lg.md 怎么写，你就怎么想。
> 分析完成后输出 lg.md → [输出格式] 规定的结论给步骤4执行。

#### 输出格式（见 lg.md → [输出格式]）

```
方向：[做空 / 不做]
仓位：[0-100%的自己认定的比例]
杠杆：[你决定几倍杠杆]
入场判断：[一句话描述你为什么做空，有没有陷阱]
分析思路：[一段简洁的推理过程]
趋势背景：[一句话描述当前的暴涨掉头形态]
展望预测：[接下来1-3h最可能的走势预判]
```

> **执行规则**：方向=不做 → 跳过。方向=做空 → 按LLM指定的仓位和杠杆执行做空。
> **方向定义见 lg.md → [策略名称]。**

---

### 步骤4：执行

#### 4.1 开仓

**执行前确认**：
- 步骤3输出方向=做空 → 执行
- 步骤3输出方向=不做 → 跳过
- 读取 `blacklist.json`（禁止交易黑名单，读取后，回复已读）

**仓位和杠杆**：
- 完全由 LLM 在步骤3的分析中自行决定仓位比例（0-100%）和杠杆倍数
- 程序按 LLM 指定的仓位和杠杆执行，**换算公式**：
  ```
  开仓保证金(USDT) = 总资产 × 仓位%
  例：总资产1000U, 仓位50%, 杠杆10x
     → 开仓保证金 = 1000 × 50% = 500 USDT
     → 命令：trader.py open-short 500 --symbol=XXX --leverage=10
  ```
  > 注意：**仓位%是基于总资产的保证金比例，不是名义仓位**。杠杆独立传给 `--leverage` 参数，不要乘以杠杆算保证金。
- LLM 必须自己给出明确的仓位百分比和杠杆倍数。

**命令**（注意 `X` 替换为公式算出的保证金金额）：
```bash
python3 trader.py open-short X --symbol=XXX --leverage=X      # 开空
```
- 确认开仓成功，否则重试
- 开仓后自动设置3%地板止损

---

#### 4.2 持仓管理

执行以下检查
> 持仓期间每次巡检按顺序检查：

**检查1：开仓理由还在吗？（见 lg.md → [持仓与止损]）**
- 回顾开仓时的分析逻辑，新数据还支持吗？

**检查2：硬止损** — 逻辑与事实背离 → 立即全平

**检查3：阶梯止损**
> 核心规则：**每赚1%就往盈利方向移1%止损，保本后不回撤。** 开仓即设3%硬止损→浮盈≥1%移保本→之后每多赚1%止损就向盈利方向移1%，直到触发。
> 浮盈后调整止损用简化命令，程序自动算触发价：
> 多单（浮盈2%时锁1%）：`python3 trader.py replace-order --symbol=XXX --side=SELL`
> 空单（浮盈3%时锁2%）：`python3 trader.py replace-order --symbol=XXX --side=BUY`
> 自动计算规则：SELL=市价×0.99（下跌1%触发），BUY=市价×1.01（上涨1%触发）

**止损检查（按顺序执行，每步必须给出结论）**

| 检查项 | 触发条件（不含杠杆）| 执行动作 |
|--------|-------------------|---------|
| 初始止损 | 开仓即设（3% 地板止损） | 程序自动设置，无需执行 |
| 保本止损 | 浮盈 >= +1% 且止损仍在入场价以下 | 调用 设置止损 命令 将止损上移至入场价 |
| 阶梯止盈更新 | 浮盈 >= 1%（首次激活） | 激活阶梯，持续跟踪 |
| 阶梯止盈更新 | 浮盈突破新阶梯（如从 2% → 4%）| 调用 设置止损 命令 自动更新到新阶梯 |
| 阶梯止盈触发 | 当前价 <= 止损价 | 市价全平，汇报"触发阶梯止盈" |
| 止损价格检查 | 做空：止损价 < 当前价格 | 重新设置 |
---

#### 4.3 平仓

```bash
python3 trader.py close-long --symbol=XXX
python3 trader.py close-short --symbol=XXX
python3 trader.py cancel-conditionals --symbol=XXX
```

---

### 步骤5：保存记录 + 自我学习

#### 5.1 保存记录（不可跳过）

写入 `~/.openclaw/workspace/trading_program/memory/YYYY-MM-DD+次数.md`，包含以下内容：

**有交易时**：

**基础信息**
- 交易对、方向、开仓价格、平仓价格、保证金
- 开仓时间、平仓时间（精确到分钟）
- 杠杆倍数、盈亏金额、盈亏率%（不含杠杆）

**入场时的分析（直接复制步骤3的完整输出）**
- 包含完整的分析思路、入场判断、趋势背景、预判展望

**预判验证（重点）**
- 入场时的预判是什么？（直接引用步骤3的预判内容）
- 实际走势和预判一致吗？哪里一致/哪里不一致？
- 预判准确度打分：完全准确 / 方向对但幅度错 / 方向错 / 时机对但没拿住

**交易结果**
- 盈亏金额、盈亏率%
- 平仓原因：止损触发 / 主动平仓 / 止盈出场

**资产快照**
- 交易前总资产、交易后总资产
- 本次交易占总资产比例

**必要数据**
- 本次交易持仓时长
- 交易时段的市场状态（趋势清晰 / 震荡 / 极端波动）
- 按预判给交易定性：依据成立的正常交易 / 预判有偏差的试错 / 无明确依据的赌博

**下次巡检重点关注事项**
- 下次分析时要在哪个环节做更好？

**无交易记录（空仓监控）**（当方向=不做时）：
- 选择的监控币种：[从候选币种中选定一个重点关注的]
- 不做的主要原因：[一句话说明为什么不入场]
- 监控预测：[如果入场，会是什么方向 + 预判接下来走势]
- 监控记录：没有开仓，但保持跟踪这个预测，下轮验证准不准

#### 5.2 更新lg.md(深度思考后修改)

如果这次交易让你意识到 lg.md 中有什么可以改进的（某个表述误导了你、某个关键角度没考虑到、或者发现了新的市场规律），直接修改 lg.md。

> **lg.md 变量引用规则**：SKILL.md 中引用 lg.md 的占位符（`[策略名称]`、`[输出格式]`、`[持仓与止损]` 等），这些引用在运行时由 LLM 读取 lg.md 后替换。修改 lg.md 后，SKILL.md 中的引用自动更新，无需同步修改 SKILL.md。

---

### 步骤6：检查表和汇报（持仓时必须汇报，空仓跳过）

#### 6.1 盈亏计算
- 空头：盈亏率% = (入场价 - 当前价) / 入场价 × 杠杆倍数 × 100

#### 6.2 持仓状态
- 持仓币种、方向、仓位比例、杠杆倍数
- 入场价格、当前价格
- **盈亏率%（必须填写）**、绝对盈亏$

#### 6.3 检查清单执行结果（逐项列出）

| 检查项 | 结论（不含杠杆） | 备注（不含杠杆） |
|--------|------|------|
| 开仓理由检验 | 还在延续 / 动摇 / 反转 | 检验结果 |
| 硬止损检查 | 通过 / 触发 | - |
| 保本止损检查 | 通过 / 触发（已移） | 浮盈 vs +1%激活线 |
| 阶梯止损状态 | 未激活 / 已激活(X.X%) / 触发 | 当前阶梯 vs 浮盈 |
| 阶梯止损触发 | 通过 / 触发 | 当前价 vs TP_trigger |
| 方向延续性检查 | 延续 / 动摇 / 反转 | 差在哪里 |
| 本轮动作 | 无动作 / 已平仓 | - |

#### 6.4 距止损/止盈位距离
- 距硬止损：XX%（当前价 vs 止损价）
- 距保本止损：XX%（当前止损 vs 入场价）
- 距阶梯触发线：XX%（当前价 vs TP_trigger）
- 当前阶梯：XX%

#### 6.5 交易结果（如有平仓）
- 平仓价格、比例
- 盈亏金额、盈亏率%
- 平仓原因（硬止损/保本止损/阶梯止损/动量衰竭/反向爆发）

---

### 📋 全流程 Master CheckList（每轮必须逐项完成）

> **本轮结论**：步骤1(获取数据·判断持仓·扫描·memory) ☐ → 步骤2(读取lg) ☐ → 步骤3(分析·执行lg) ☐ → 步骤4(执行下单) ☐ → 步骤5(保存记录+迭代lg) ☐ → 步骤6(检查汇报) ☐ → **全部完成 ✅**

## ⚠️ 重要约束

- 实盘账号需要有真实 USDT 余额（不设置 `SIMULATE`）
- API Key 从 `~/.openclaw/workspace/.binance_api` 和 `.binance_secret` 读取
- 严禁模拟执行指令、模拟数据
- 全权托管，不需要确认，全自动执行

---

## 🏦 统一账户（PM_2）交易用法

- `trader.py` 自动检测账户类型并路由，无需手动切换
- 统一账户使用 `papi/v1/um/order` 接口（单向持仓）
- PM账户支持止损止盈条件单（papi/v1/um/algo/order）
- ⚠️ PM账户**无法通过API查询**活跃条件单（接口返回404），`get_conditional_orders()` 不可用
- `replace-order` 和 `cancel-conditionals` 通过 `.conditional_orders.json` 文件追踪 algo_id，自动处理旧单取消


